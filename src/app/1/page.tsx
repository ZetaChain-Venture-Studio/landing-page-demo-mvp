import LandingPage from '@/components/LandingPage';
import { modernMinimalism } from '@/lib/palettes';

export default function Palette1Page() {
  return <LandingPage paletteId="1" palette={modernMinimalism} />;
}
