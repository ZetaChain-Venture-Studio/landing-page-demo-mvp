import LandingPage from '@/components/LandingPage';
import { cyberNeon } from '@/lib/palettes';

export default function Palette6Page() {
  return <LandingPage paletteId="6" palette={cyberNeon} />;
}
